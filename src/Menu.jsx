import { Button } from 'react-bootstrap'

const Menu = () => {
  return (
    <div className="Menu">
        <Button>Map</Button>
        <Button>Camera</Button>
        <Button>Log</Button>
        <Button>Account</Button>
    </div>
  );
};

export default Menu;